# Default inbox.
set spoolfile = "+cern/INBOX"

# Other special folders.
set mbox      = "+cern/archive"
set record    = "+cern/Sent\ Items"
set postponed = "+cern/Drafts"

set from     = "ramon.niet@cern.ch"
set sendmail = "/usr/bin/msmtp -a cern"
set sendmail_wait = 0
unset record

#vim: ft=muttrc
