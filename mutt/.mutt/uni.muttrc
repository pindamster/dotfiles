# Default inbox.
set spoolfile = "+uni/INBOX"

# Other special folders.
set mbox      = "+uni/archive"
set record    = "+uni/sent"
set postponed = "+uni/drafts"

set from     = "Ramon.Niet@tu-dortmund.de"
set sendmail = "/usr/bin/msmtp -a uni"
set sendmail_wait = 0
unset record

#vim: ft=muttrc
